{
  "server": {
    "address": "{{ or .SERVER_ADDR `localhost:8000`}}",
    "timeout": "{{ or .SERVER_TIMEOUT `4s`}}",
    "idle_timeout": "{{ or .SERVER_IDLE_TIMEOUT `60s`}}"
  },
  "app_env": "{{ or .APP_ENV  `local`}}",
  "database": {
    "host": "{{ or .DB_HOST `mysql`}}"
  }
}